import { sections } from "../../utils/sections";

export const About = () => {
  return (
    <div id={sections.about.id} className="min-h-screen bg-slate-50 pt-20">
      <h2 className="font-brand text-4xl font-medium">About</h2>
    </div>
  );
};
